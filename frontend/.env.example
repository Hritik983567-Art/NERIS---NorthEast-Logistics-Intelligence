# NERIS Frontend Environment Configuration Example (Placeholders Only)
# Copy to frontend/.env for local overrides. Never commit secrets to Git.

VITE_API_BASE_URL=
VITE_DEMO_MODE=false
VITE_AWS_REGION=us-east-1
VITE_COGNITO_USER_POOL_ID=
VITE_COGNITO_CLIENT_ID=
